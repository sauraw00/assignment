
            Start editing…HELP STUDY ABROADFrontend Technical AssessmentTime Allotment: 3 HoursObjective:To assess the candidate’s practical skills in building a modern, responsive, and efficient web application using Next.js, Zustand, MUI, and integrating REST APIs.Instructions for Candidates* You must create a new Next.js project.* Use Material-UI (MUI) for UI components.* Use Zustand for state management.* All backend data must come from the public API:&nbsp;&nbsp;[https://dummyjson.com/](https://dummyjson.com/)* After completion, create a public GitHub repo, push your project, and share the link.* Prioritize correctness, clean code, organization, and responsive UI.* If any part is incomplete, document what’s done and what’s pending.---Part 1: Frontend Development (2 Hours)1a. Authentication (30 minutes)Use DummyJSON’s built-in auth API:`POST [https://dummyjson.com/auth/login`](https://dummyjson.com/auth/login)Tasks:* Build an admin login page using MUI.* Call the DummyJSON login API and authenticate the user.* use nextAuth for login &amp; Store the token in Zustand state (and optionally localStorage).* Redirect authenticated users to the dashboard.* Protect dashboard routes so unauthenticated users cannot access them.---1b. Users List + Single User View (45 minutes)Users List PageUse DummyJSON API:* List Users: `GET [https://dummyjson.com/users?limit=10&amp;skip=0`](https://dummyjson.com/users?limit=10&amp;skip=0)* Search Users: `GET [https://dummyjson.com/users/search?q=...`](https://dummyjson.com/users/search?q=...)Requirements:* Display list of users in a responsive MUI table or card UI.* Implement:&nbsp;&nbsp;* Pagination using `limit` and `skip`&nbsp;&nbsp;* Search filter* Show user’s name, email, gender, phone, and company.### Single User PageUse:`GET [https://dummyjson.com/users/{id}`](https://dummyjson.com/users/%7Bid%7D)Requirements:* Show full user details in a clean, organized MUI layout.* Provide a “Back to Users” link.---1c. Products List + Single Product View (45 minutes)Products List PageUse:* Products: `GET [https://dummyjson.com/products?limit=10&amp;skip=0`](https://dummyjson.com/products?limit=10&amp;skip=0)* Search: `GET [https://dummyjson.com/products/search?q=...`](https://dummyjson.com/products/search?q=...)* Category filter: `GET [https://dummyjson.com/products/category/{category}`](https://dummyjson.com/products/category/%7Bcategory%7D)Requirements:* Display products in a responsive MUI grid layout.* Implement:&nbsp;&nbsp;* **Pagination**&nbsp;&nbsp;* **Search bar**&nbsp;&nbsp;* **Category filter dropdown*** Show product image, title, price, category, rating.Single Product PageUse:`GET [https://dummyjson.com/products/{id}`](https://dummyjson.com/products/%7Bid%7D)Requirements:* Display full product details (images carousel, description, specs).* Provide “Back to Products” link.---Part 2: State Management with Zustand (30 minutes)Tasks:* Use Zustand to manage:&nbsp;&nbsp;* User authentication state&nbsp;&nbsp;* Users data state&nbsp;&nbsp;* Products data state* Implement async actions inside Zustand store for API calls.* Explain in a comment or README **why Zustand was chosen** (simplicity, small footprint, built-in async actions, better than Redux for small–medium apps).---Part 3: UI/UX &amp; Optimization (30 minutes)3a. UI &amp; Responsiveness* Entire UI must use Material-UI.* Ensure responsive layouts for:&nbsp;&nbsp;* Login page&nbsp;&nbsp;* Users list page&nbsp;&nbsp;* Products list page&nbsp;&nbsp;* Detail pages3b. Performance Optimization* Use React.memo where beneficial.* Use `useCallback` / `useMemo` to reduce unnecessary re-renders.* Use API-side pagination instead of loading large lists.3c. Client-Side Caching* Cache list results using Zustand or localStorage to avoid repeat API calls.* Add comments explaining:&nbsp;&nbsp;* Why caching is useful&nbsp;&nbsp;* What caching strategy you implemented---Submission Guidelines* Create a public GitHub repository.* Add a README with:&nbsp;&nbsp;* Setup instructions&nbsp;&nbsp;* Installation &amp; run commands&nbsp;&nbsp;* Environment variable instructions (if using token storage)* Share your GitHub link via email or message.---Evaluation CriteriaYou will be evaluated on:1. Functionality:&nbsp;&nbsp;&nbsp;* Auth works&nbsp;&nbsp;&nbsp;* Pagination &amp; search work&nbsp;&nbsp;&nbsp;* Filters work&nbsp;&nbsp;&nbsp;* Detail pages work2. Code Quality:&nbsp;&nbsp;&nbsp;* Clean, readable, modular&nbsp;&nbsp;&nbsp;* Proper folder structure&nbsp;&nbsp;&nbsp;* Meaningful variables &amp; components3. State Management:&nbsp;&nbsp;&nbsp;* Proper Zustand implementation&nbsp;&nbsp;&nbsp;* Async API calls handled cleanly4. UI/UX:&nbsp;&nbsp;&nbsp;* MUI components&nbsp;&nbsp;&nbsp;* Responsive design&nbsp;&nbsp;&nbsp;* Clean layouts5. Performance Optimization6. Documentation &amp; Comments